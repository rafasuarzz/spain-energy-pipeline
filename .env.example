# Copy to .env and adjust if needed. No secrets are required: the REE API is public.
AIRFLOW_UID=50000
# Shared JWT secret for Airflow 3 components. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
AIRFLOW_JWT_SECRET=change-me
ENERGY_DUCKDB_PATH=./data/warehouse/energy.duckdb
