name: spain_energy
version: "1.0.0"
profile: spain_energy

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: target
clean-targets: ["target", "dbt_packages"]

models:
  spain_energy:
    staging:
      +materialized: view
    marts:
      +materialized: table
